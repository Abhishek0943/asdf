# Example environment configuration for the Node.js API
# Copy this file to ".env" and fill in the actual values.

# -----------------------------------------------------------
# Application
# -----------------------------------------------------------
# The environment the app is running in
# Options: development | production | test
NODE_ENV=development

# Port on which your Node.js app will run
PORT=8000


# -----------------------------------------------------------
# Database (MongoDB)
# -----------------------------------------------------------

# Local MongoDB connection URI (used for local development)
DB_URI_LOCAL=mongodb://127.0.0.1:27017/<db-name>

# Production MongoDB connection URI (use your own credentials)
DB_URI_PRO=mongodb+srv://<username>:<password>@cluster0.<your-cluster>.mongodb.net/<db-name>


# -----------------------------------------------------------
# JWT / Secrets / Keys
# -----------------------------------------------------------
# Secret key used for signing JSON Web Tokens
JWT_SECRET=your_jwt_secret_here
